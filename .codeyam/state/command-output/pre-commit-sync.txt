{"command":"pre-commit-sync","elapsed_secs":0,"emitted_at_unix":1786369305,"heartbeat":"CODEYAM_CMD_RUNNING","note":"still running","pid":2197761}
⚠️  CODEYAM: stdout is piped through tail/grep — the consumer block-buffers its input to EOF, so this task's `.output` would be empty until the command exits. Liveness is being mirrored to stderr (below) so it stays visible.
> liveness is mirrored to stderr (this file) because stdout is piped through tail/grep. Run this command BARE instead and block with `codeyam-editor editor wait-for <task .output path printed by the harness>` — a pipe reports the filter's exit code, not the command's. If you piped it to shorten the output, you did not need to: run bare and the command prints a `CODEYAM_FULL_OUTPUT` line naming a file that holds the COMPLETE output, which you can `cat` afterwards (or capture with `tee` if you want both at once).
{"command":"pre-commit-sync","elapsed_secs":0,"emitted_at_unix":1786369305,"heartbeat":"CODEYAM_CMD_RUNNING","note":"still running","pid":2197761}
Commit queue: enqueuing on branch 'main'... (normal; serializes pushes when multiple sessions commit concurrently. See docs/commit-queue.md.)
Commit queue: enqueued (uuid=fcdd68e9-26f7-4592-af3f-d309f5990bca). Waiting for head. If you're the only session this clears in ~1s; otherwise inspect with `codeyam-editor editor branch-queue status`.
Commit queue: now BLOCKING until this session reaches the head of the queue on 'main'. This is EXPECTED, not a hang — it may take a while on a deep queue. You will be returned automatically the moment head is acquired — that automatic return IS your completion notification, so there is nothing to poll for. Periodic `Commit queue: still waiting — position N of M …` lines are normal progress. Run this ONCE and let it block: do NOT poll `branch-queue status` in a loop, do NOT re-read queue position on a timer, do NOT re-run the command, and do NOT ask the operator whether to wait or skip. The operator can interrupt the command to force a skip. (See docs/commit-queue.md.)
✅ Commit queue: HEAD ACQUIRED after 0s — proceeding with sync. This return IS the head-acquired signal; do not poll or re-run.
Commit queue: head acquired (uuid=fcdd68e9-26f7-4592-af3f-d309f5990bca). Proceeding now — the blocking wait is over; the tail will run under serialized order.
Commit queue: head acquired (uuid=fcdd68e9-26f7-4592-af3f-d309f5990bca, branch=main, remote=origin). Tail will run under serialized order. (See docs/commit-queue.md.)
{"commitsPulled":0,"fastPathReason":"ls-remote matched local @{upstream}","headAcquired":true,"pulled":false,"synced":true,"waitedSec":0}
