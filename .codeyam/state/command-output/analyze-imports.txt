> auto-backgrounded — the harness completion notification arrives on its own when this exits; you do NOT need to do anything to receive it, and waiting adds nothing. ONLY if you need the result inside the CURRENT turn, block with: codeyam-editor editor wait-for /tmp/claude-0/-workspace/1e3968f4-d648-4e10-96a7-4ac0e3be8109/tasks/bc1t2pml5.output (ONE call, bare — never piped through `| tail`; its budget is capped to fit the tool-call ceiling, so a long run returns WAIT_FOR STILL_RUNNING in-call and the notification still arrives. Re-issue only on LIKELY_WEDGED; do NOT hand-roll an `until grep … sleep` loop, the harness blocks it). Output too long? You do not need `| tail`: run it bare and read the `CODEYAM_FULL_OUTPUT` line the command prints — it names a file holding the COMPLETE output, which you can `cat` afterwards.
{"command":"analyze-imports","elapsed_secs":0,"emitted_at_unix":1786554160,"heartbeat":"CODEYAM_CMD_RUNNING","note":"still running","pid":3725750}
analyze-imports: scoped to 83 changed / 3 deleted file(s) since HEAD
{
Analyzed 18 files, found 60 import edges, graph has 343 nodes
  "analyzed": 18,
  "entityCount": 426,
  "graphSize": 343,
  "totalImports": 60
}
