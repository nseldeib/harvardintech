⚠️  CODEYAM: stdout is piped through tail/grep — the consumer block-buffers its input to EOF, so this task's `.output` would be empty until the command exits. Liveness is being mirrored to stderr (below) so it stays visible.
{"command":"refresh-tests","elapsed_secs":0,"emitted_at_unix":1786131210,"heartbeat":"CODEYAM_CMD_RUNNING","note":"still running","pid":106500}
> liveness is mirrored to stderr (this file) because stdout is piped through tail/grep. Run this command BARE instead and block with `codeyam-editor editor wait-for <task .output path printed by the harness>` — a pipe reports the filter's exit code, not the command's. If you piped it to shorten the output, you did not need to: run bare and the command prints a `CODEYAM_FULL_OUTPUT` line naming a file that holds the COMPLETE output, which you can `cat` afterwards (or capture with `tee` if you want both at once).
{"command":"refresh-tests","elapsed_secs":0,"emitted_at_unix":1786131210,"heartbeat":"CODEYAM_CMD_RUNNING","note":"still running","pid":106500}
[refresh-tests --changed] scope: 16 changed path(s) (44 untracked included)
[refresh-tests --changed]   matched runner(s): tests

action required: 1 brand-new test file(s) ran scoped to themselves, but will NOT reach the audit-gate cache via --changed — re-run with --force before finalize
note: --changed is a partial run: it updates only the affected partition cache(s),
note: NOT the audit-gate full-suite cache. The untracked test file(s):
  src/lib/lightbox.test.ts
note: to land them in the gated full-suite cache (required before finalize), run:
note:   codeyam-editor editor refresh-tests --force
{
  "analyzed": 15,
Analyzed 15 files, found 42 import edges, graph has 313 nodes
  "entityCount": 389,
  "graphSize": 313,
  "totalImports": 42
}

note: dependency graph was stale — auto-refreshed 15 changed file(s) so --changed maps them this run:
  src/components/ChapterPage.astro
  src/components/landing/EventGallery.astro
  src/components/landing/GalleryExpand.astro
  src/components/landing/GalleryLightbox.astro
  src/components/landing/GalleryTile.astro
  src/content/config.ts
  src/lib/gallery.test.ts
  src/lib/gallery.ts
  src/lib/lightbox.test.ts
  src/lib/lightbox.ts
  src/pages/chapters/[slug].astro
  src/pages/isolated-components/GalleryExpand.astro
  src/pages/isolated-components/GalleryLightbox.astro
  src/pages/isolated-components/GalleryLightboxUncaptioned.astro
  src/pages/isolated-components/GalleryTile.astro
note: the change→test mapping is now current; no analyze-imports or raw-runner fallback is needed.
Running tests via the configured runner command(s) — use these, do not reconstruct runner flags:
  tests  ::  npx vitest run --reporter=json
Test refresh triggered. Waiting for results...
[   2s] Running runner: tests
[   4s] Tests completed.
All 45 tests passed.
{"failed":0,"passed":45,"skipped":0,"success":true,"total":45}

Partial run — partition caches WERE updated; the full-suite test-cache.json was
NOT (full-suite hash invariant). If a stale-partition bail sent you here, that
recovery has landed — re-run the command that bailed.
Audit gates read the full-suite cache. Flag-free `refresh-tests` updates it but
is gated until `finalize`. To update the full-suite cache now (e.g. for
brand-new tests), run: codeyam-editor editor refresh-tests --force
